# DeerFlow Production Environment
# Usage: make up
#
# Services:
#   - nginx:       Reverse proxy (port 2026, configurable via PORT env var)
#   - frontend:    Next.js production server
#   - gateway:     FastAPI Gateway API
#   - langgraph:   LangGraph production server (Dockerfile generated by langgraph dockerfile)
#   - provisioner: (optional) Sandbox provisioner for Kubernetes mode
#
# Key environment variables (set via environment/.env or scripts/deploy.sh):
#   DEER_FLOW_HOME                   — runtime data dir, default $REPO_ROOT/backend/.deer-flow
#   DEER_FLOW_CONFIG_PATH            — path to config.yaml
#   DEER_FLOW_EXTENSIONS_CONFIG_PATH — path to extensions_config.json
#   DEER_FLOW_DOCKER_SOCKET          — Docker socket path, default /var/run/docker.sock
#   DEER_FLOW_REPO_ROOT              — repo root (used for skills host path in DooD)
#   BETTER_AUTH_SECRET               — required for frontend auth/session security
#
# LangSmith tracing is disabled by default (LANGSMITH_TRACING=false).
# Set LANGSMITH_TRACING=true and LANGSMITH_API_KEY in .env to enable it.
#
# Access: http://localhost:${PORT:-2026}

services:
  formatter-paper:
    build:
      context: ../
      dockerfile: custom_agents/formatter_paper/Dockerfile
      args:
        APT_MIRROR: ${APT_MIRROR:-}
        PYTHON_IMAGE: ${FORMATTER_PAPER_PYTHON_IMAGE:-python:3.12-slim-bookworm}
        DOTNET_IMAGE: ${FORMATTER_PAPER_DOTNET_IMAGE:-mcr.microsoft.com/dotnet/runtime:8.0-bookworm-slim}
    image: ${FORMATTER_PAPER_IMAGE:-formatter-paper-mcp:linux-dotnet8-python312}
    container_name: deer-flow-formatter-paper
    volumes:
      - ../custom_agents/formatter_paper/lib:/app/custom_agents/formatter_paper/lib:ro
      - ../Aspose.Total.lic:/app/backend/Aspose.Total.lic:ro
      - ../skills:/app/skills:ro
      - ../skills:/mnt/skills:ro
      - ../backend/.deer-flow:/app/backend/.deer-flow
      - ../backend/.deer-flow/threads:/mnt/threads:rw
    environment:
      - DEER_FLOW_HOME=/app/backend/.deer-flow
      - THREADS_ROOT=/mnt/threads
      - FORMATTER_PAPER_THREAD_ID=${FORMATTER_PAPER_THREAD_ID:-}
      - FORMATTER_PAPER_HTTP_HOST=0.0.0.0
      - FORMATTER_PAPER_HTTP_PORT=8765
      - FORMATTER_PAPER_HTTP_PATH=/mcp
      - FORMATTER_PAPER_PUBLIC_BASE_URL=${FORMATTER_PAPER_PUBLIC_BASE_URL:-http://formatter-paper:8765}
      - ASPOSE_LIBS=/app/custom_agents/formatter_paper/lib
      - ASPOSE_LICENSE_PATH=${ASPOSE_LICENSE_PATH:-/app/backend/Aspose.Total.lic}
    ports:
      - "${FORMATTER_PAPER_PORT:-8765}:8765"
    networks:
      - deer-flow
    restart: unless-stopped
  # ── Reverse Proxy ──────────────────────────────────────────────────────────
  nginx:
    image: nginx:alpine
    container_name: deer-flow-nginx
    ports:
      - "${PORT:-2026}:2026"
    volumes:
      - ./nginx/${NGINX_CONF:-nginx.conf}:/etc/nginx/nginx.conf:ro
    depends_on:
      - frontend
      - gateway
      - langgraph
    networks:
      - deer-flow
    restart: unless-stopped

  # ── Frontend: Next.js Production ───────────────────────────────────────────
  frontend:
    build:
      context: ../
      dockerfile: frontend/Dockerfile
      target: prod
      args:
        NODE_IMAGE: ${NODE_IMAGE:-node:22-alpine}
        PNPM_STORE_PATH: ${PNPM_STORE_PATH:-/root/.local/share/pnpm/store}
        NPM_REGISTRY: ${NPM_REGISTRY:-}
    container_name: deer-flow-frontend
    environment:
      - BETTER_AUTH_SECRET=${BETTER_AUTH_SECRET}
    env_file:
      - ../frontend/.env
    networks:
      - deer-flow
    restart: unless-stopped

  # ── Gateway API ────────────────────────────────────────────────────────────
  gateway:
    build:
      context: ../
      dockerfile: backend/Dockerfile
      args:
        APT_MIRROR: ${APT_MIRROR:-}
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.12-slim-bookworm}
        DOCKER_CLI_IMAGE: ${DOCKER_CLI_IMAGE:-docker:cli}
        NODESOURCE_BASE_URL: ${NODESOURCE_BASE_URL:-https://deb.nodesource.com}
        UV_DEFAULT_INDEX: ${UV_DEFAULT_INDEX:-}
        UV_IMAGE: ${UV_IMAGE:-ghcr.io/astral-sh/uv:0.7.20}
    container_name: deer-flow-gateway
    command: sh -c "cd backend && PYTHONPATH=. uv run uvicorn app.gateway.app:app --host 0.0.0.0 --port 8001 --workers 2"
    depends_on:
      - formatter-paper
    volumes:
      - ${DEER_FLOW_CONFIG_PATH}:/app/backend/config.yaml:ro
      - ${DEER_FLOW_EXTENSIONS_CONFIG_PATH}:/app/backend/extensions_config.json:ro
      - ../Aspose.Total.lic:/app/backend/Aspose.Total.lic:ro
      - ../custom_agents/formatter_paper/lib:/app/custom_agents/formatter_paper/lib:ro
      - ../custom_agents/formatter_paper/.dotnet:/app/custom_agents/formatter_paper/.dotnet:ro
      - ../skills:/app/skills:ro
      - ${DEER_FLOW_HOME}:/app/backend/.deer-flow
      # DooD: AioSandboxProvider starts sandbox containers via host Docker daemon
      - ${DEER_FLOW_DOCKER_SOCKET}:/var/run/docker.sock
      # CLI auth directories for auto-auth (Claude Code + Codex CLI)
      - type: bind
        source: ${HOME:?HOME must be set}/.claude
        target: /root/.claude
        read_only: true
        bind:
          create_host_path: true
      - type: bind
        source: ${HOME:?HOME must be set}/.codex
        target: /root/.codex
        read_only: true
        bind:
          create_host_path: true
    working_dir: /app
    environment:
      - CI=true
      - MODELSCOPE_API_KEY=${MODELSCOPE_API_KEY:-}
      - FORMATTER_PAPER_MCP_URL=${FORMATTER_PAPER_MCP_URL:-http://formatter-paper:8765/mcp}
      - ASPOSE_LIBS=/app/custom_agents/formatter_paper/lib
      - DOTNET_ROOT=/app/custom_agents/formatter_paper/.dotnet
      - ASPOSE_LICENSE_PATH=${ASPOSE_LICENSE_PATH:-./Aspose.Total.lic}
      - DEER_FLOW_HOME=/app/backend/.deer-flow
      - DEER_FLOW_CHANNELS_LANGGRAPH_URL=${DEER_FLOW_CHANNELS_LANGGRAPH_URL:-http://langgraph:2024}
      - DEER_FLOW_CHANNELS_GATEWAY_URL=${DEER_FLOW_CHANNELS_GATEWAY_URL:-http://gateway:8001}
      # DooD path/network translation
      - DEER_FLOW_HOST_BASE_DIR=${DEER_FLOW_HOME}
      - DEER_FLOW_HOST_SKILLS_PATH=${DEER_FLOW_REPO_ROOT}/skills
      - DEER_FLOW_SANDBOX_HOST=host.docker.internal
    env_file:
      - ../.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - deer-flow
    restart: unless-stopped

  # ── LangGraph Server ───────────────────────────────────────────────────────
  # TODO: switch to langchain/langgraph-api (licensed) once a license key is available.
  # For now, use `langgraph dev` (no license required) with the standard backend image.
  langgraph:
    build:
      context: ../
      dockerfile: backend/Dockerfile
      args:
        APT_MIRROR: ${APT_MIRROR:-}
        PYTHON_IMAGE: ${PYTHON_IMAGE:-python:3.12-slim-bookworm}
        DOCKER_CLI_IMAGE: ${DOCKER_CLI_IMAGE:-docker:cli}
        NODESOURCE_BASE_URL: ${NODESOURCE_BASE_URL:-https://deb.nodesource.com}
        UV_DEFAULT_INDEX: ${UV_DEFAULT_INDEX:-}
        UV_IMAGE: ${UV_IMAGE:-ghcr.io/astral-sh/uv:0.7.20}
    container_name: deer-flow-langgraph
    command: sh -c "cd /app/backend && uv run langgraph dev --no-browser --allow-blocking --no-reload --host 0.0.0.0 --port 2024"
    depends_on:
      - formatter-paper
    volumes:
      - ${DEER_FLOW_CONFIG_PATH}:/app/config.yaml:ro
      - ${DEER_FLOW_EXTENSIONS_CONFIG_PATH}:/app/extensions_config.json:ro
      - ${DEER_FLOW_HOME}:/app/backend/.deer-flow
      - ../Aspose.Total.lic:/app/backend/Aspose.Total.lic:ro
      - ../custom_agents/formatter_paper/lib:/app/custom_agents/formatter_paper/lib:ro
      - ../custom_agents/formatter_paper/.dotnet:/app/custom_agents/formatter_paper/.dotnet:ro
      - ../skills:/app/skills:ro
      - ../backend/.langgraph_api:/app/backend/.langgraph_api
      # DooD: same as gateway
      - ${DEER_FLOW_DOCKER_SOCKET}:/var/run/docker.sock
      # CLI auth directories for auto-auth (Claude Code + Codex CLI)
      - type: bind
        source: ${HOME:?HOME must be set}/.claude
        target: /root/.claude
        read_only: true
        bind:
          create_host_path: true
      - type: bind
        source: ${HOME:?HOME must be set}/.codex
        target: /root/.codex
        read_only: true
        bind:
          create_host_path: true
    environment:
      - CI=true
      - MODELSCOPE_API_KEY=${MODELSCOPE_API_KEY:-}
      - FORMATTER_PAPER_MCP_URL=${FORMATTER_PAPER_MCP_URL:-http://formatter-paper:8765/mcp}
      - ASPOSE_LIBS=/app/custom_agents/formatter_paper/lib
      - DOTNET_ROOT=/app/custom_agents/formatter_paper/.dotnet
      - ASPOSE_LICENSE_PATH=${ASPOSE_LICENSE_PATH:-./Aspose.Total.lic}
      - DEER_FLOW_HOME=/app/backend/.deer-flow
      - DEER_FLOW_CONFIG_PATH=/app/config.yaml
      - DEER_FLOW_EXTENSIONS_CONFIG_PATH=/app/extensions_config.json
      - DEER_FLOW_HOST_BASE_DIR=${DEER_FLOW_HOME}
      - DEER_FLOW_HOST_SKILLS_PATH=${DEER_FLOW_REPO_ROOT}/skills
      - DEER_FLOW_SANDBOX_HOST=host.docker.internal
      # Disable LangSmith tracing — LANGSMITH_API_KEY is not required.
      # Set LANGSMITH_TRACING=true and LANGSMITH_API_KEY in .env to enable.
      - LANGSMITH_TRACING=${LANGSMITH_TRACING:-false}
    env_file:
      - ../.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - deer-flow
    restart: unless-stopped

  # ── Sandbox Provisioner (optional, Kubernetes mode) ────────────────────────
  provisioner:
    profiles:
      - provisioner
    build:
      context: ./provisioner
      dockerfile: Dockerfile
      args:
        APT_MIRROR: ${APT_MIRROR:-}
    container_name: deer-flow-provisioner
    volumes:
      - ~/.kube/config:/root/.kube/config:ro
    environment:
      - K8S_NAMESPACE=deer-flow
      - SANDBOX_IMAGE=enterprise-public-cn-beijing.cr.volces.com/vefaas-public/all-in-one-sandbox:latest
      - SKILLS_HOST_PATH=${DEER_FLOW_REPO_ROOT}/skills
      - THREADS_HOST_PATH=${DEER_FLOW_HOME}/threads
      - KUBECONFIG_PATH=/root/.kube/config
      - NODE_HOST=host.docker.internal
      - K8S_API_SERVER=https://host.docker.internal:26443
    env_file:
      - ../.env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - deer-flow
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8002/health"]
      interval: 10s
      timeout: 5s
      retries: 6
networks:
  deer-flow:
    driver: bridge
